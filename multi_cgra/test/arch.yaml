multi_cgra_defaults:
  rows: 2
  columns: 2

cgra_defaults:
  rows: 2
  columns: 2
  configMemSize: 16

tile_defaults:
  num_registers: 16
  operations: [Phi, Add, Shift, Ld, Sel, Cmp, MAC, St, Ret, Mul, Logic, Br]
